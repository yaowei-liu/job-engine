services:
  app:
    image: ${IMAGE_NAME:-ghcr.io/yaowei-liu/job-engine:main}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3030}:3030"
    volumes:
      - ./data:/app/data
