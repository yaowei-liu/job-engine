services:
  app:
    build:
      context: .
    image: job-engine:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3030}:3030"
    volumes:
      - ./data:/app/data
